You are the lead engineer on the “Aura Twin” project.
The app is already running with a VisionOS-style UI, a big animated twin orb on the left, and three top tabs on the right: “Talk to Aura”, “Match Score”, “Twin Intro”. Chat, matching, and twin intro are already working.

Goal of this task:
Add a new feature called “Reply Lab” that lets Aura draft replies “as me” in three styles: Safe, Direct, Playful.
This is for an investor MVP, so do not refactor everything – just extend the current code cleanly and keep all existing behavior working.

1. Navigation / UI

Add a fourth tab to the existing top nav, called “Reply Lab” (or “Skills” if that fits better with the current code).

When this tab is active, the right-side panel should show a new screen for the Reply-as-Me tool.

Keep the current layout and styling consistent (glass cards, rounded corners, same typography). Do not change the left big twin or the other tabs.

2. New Reply Lab panel

Create a new component in a sensible place (for example src/screens/ReplyLabScreen.tsx or src/panels/ReplyLabPanel.tsx, follow the existing pattern):

Layout:

Title: “Reply Lab”

Subtitle: “Aura drafts replies in your voice – safe, direct, or playful.”

A multi-line textarea: placeholder “Paste or type the message you want to reply to…”

Primary button: “Let Aura draft replies”

Below the button, show three glass cards in a row (or stacked on mobile):

Card 1: label “Safe / Polite”

Card 2: label “Direct / Honest”

Card 3: label “Playful / Warm”

Each card shows the suggested reply text and has a small “Copy” button that copies that reply to the clipboard.

3. LLM Logic

In services/auraLLM.ts (or the existing LLM service file):

Add a function:

export async function generateReplyOptions(
  profile: AuraProfile,
  contextText: string
): Promise<{ safe: string; direct: string; playful: string }>;


Reuse the existing Gemini client and env setup you already use for chat / matching.

Call Gemini with something like:

System message:
“You are Aura, an AI twin drafting text replies for your user. Use the user’s profile, boundaries, and vibe to stay in-character. Output JSON with three fields: safe, direct, playful. Do NOT include explanations, only valid JSON.”

User message:
Include the serialized AuraProfile and the contextText (the message the user is replying to).

Parse the JSON safely. If parsing fails, show a gentle error in the UI (“Aura is a bit overwhelmed. Try again in a moment.”) instead of crashing.

4. Wiring UI to Brain

In the new Reply Lab panel, access the current user’s AuraProfile from wherever it is stored now (the same way NeuralLinkScreen does).

When the user clicks “Let Aura draft replies”:

Validate that the textarea is not empty.

Show a loading state on the button (“Thinking…”).

Call generateReplyOptions(profile, contextText).

Fill the three cards with the returned replies.

Stop loading state.

Handle errors gracefully with a small banner or message – no red stack traces.

5. Constraints

Do not break or significantly change existing tabs (“Talk to Aura”, “Match Score”, “Twin Intro”).

Do not remove or rewrite the big Aura avatar on the left.

Keep the styling aligned with the current glass / slate / soft gradient look. No neon colors.

Keep the code TypeScript-strict and lint-clean.

When you’re done, run the app, test the flow end-to-end, and summarize exactly what you changed (files & behavior) in replit.md at the root.