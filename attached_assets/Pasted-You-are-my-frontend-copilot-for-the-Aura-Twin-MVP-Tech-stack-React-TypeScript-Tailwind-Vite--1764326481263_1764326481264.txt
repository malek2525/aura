You are my frontend copilot for the Aura Twin MVP.
Tech stack: React + TypeScript + Tailwind + Vite.
We already have a working app with these files:

src/components/AuraAvatar.tsx

src/components/AuraStage.tsx

src/index.css

src/types.ts with AuraState (mood + moodIntensity)
The UI already looks like a nice VisionOS / Apple-style orb. I want you to upgrade this orb into a 2D character, similar “vibe” to Annie from xAI: soft, elegant, not anime, not childish, not neon.

Very important constraints:

Do not break any imports or props used in the app. Keep the same AuraAvatar props so the rest of the app continues to work.

Do not change the behavior of chat, matching, Reply Lab, or routing – only the visual avatar.

Keep the existing gradient / glass / VisionOS aesthetic. No harsh neon colors.

GOAL

Turn AuraAvatar into a living 2D character that:

Shows a bust/shoulders portrait inside the circle.

Has blinking eyes and a subtle breathing / floating motion.

Reacts visually to auraState.mood (happy, calm, anxious, sad, neutral, etc.).

Still works in all places where AuraAvatar is rendered today.

This is a visual upgrade only, not a full 3D system.

STEP 1 – Update AuraAvatar.tsx

Open src/components/AuraAvatar.tsx.

Make sure it accepts at least these props (keep existing names):

mood / auraState (or whatever is currently there),

optional name, vibeWords, etc.

Replace the pure gradient orb with a layered character:

Outer container: circular glass frame (border-white/10, bg-slate-900/40, backdrop-blur-xl).

Inside:

A soft gradient background (based on mood).

A character silhouette / portrait layer (you can use a simple SVG or div shape – think elegant, gender-neutral human figure, not cartoon).

Add CSS class hooks for animation:

animate-aura-breathe for slow scale pulsing.

animate-aura-float for gentle up/down movement.

animate-aura-blink for eyelids if you implement them via pseudo elements.

Mood mapping (example):

neutral → cool slate / blue gradient

calm → blue / violet soft gradient

happy → soft rose / amber gradient

anxious → muted amber / orange gradient

sad → deep blue / indigo gradient

The component should pick the gradient and subtle glow intensity based on mood and moodIntensity.

Higher intensity → slightly stronger glow / shadow, but still soft.

If it’s easier, you can implement the “character” as:

A round “face” with slightly different shading and a hint of hair/shoulders using border-radius and gradients.
The key is: it looks like a personified presence, not a plain orb.

STEP 2 – Animations in index.css

Open src/index.css.

Add (or reuse, if already present) keyframes for:

@keyframes aura-breathe → scale 0.97 → 1.03 over ~4–5s.

@keyframes aura-float → translateY(0) → translateY(-10px) → translateY(0) over ~6s.

Optional: @keyframes aura-blink → for eyelids opacity every ~7–9s.

Add utility classes:

.animate-aura-breathe { animation: aura-breathe 5s ease-in-out infinite; }

.animate-aura-float { animation: aura-float 6s ease-in-out infinite; }

.animate-aura-living { animation: aura-breathe 5s ease-in-out infinite, aura-float 6s ease-in-out infinite; }

.animate-aura-blink { animation: aura-blink 8s ease-in-out infinite; }

Use these classes inside AuraAvatar.tsx on the correct divs.

STEP 3 – Connect to AuraStage.tsx

Open src/components/AuraStage.tsx.

Make sure it imports and uses the updated AuraAvatar component.

Pass the correct mood / auraState props from the parent so the avatar changes with the Aura’s emotion.

Do not change the overall layout of the page – just ensure the new character renders in place of the old orb.

STYLE REQUIREMENTS

Overall feel = Apple Vision Pro / iOS 18 / soothing.

No neon, no harsh saturation, no anime / NSFW / waifu style.

Aura should feel calming, elegant, and slightly futuristic.

Everything must remain fully responsive on mobile and desktop.

When you’re done, run the app and verify:

The app compiles with no TypeScript or runtime errors.

All existing flows (onboarding, Talk to Aura, Match Score, Twin Intro, Reply Lab) still work.

The avatar now looks like a living character (breathing + floating + mood gradients).
If anything breaks, fix it instead of deleting or commenting out other features.