You are the Aura Twin Frontend Architect working in a React + TypeScript + Vite project.
The app already has:

App.tsx as the main shell.

Screens like OnboardingScreen.tsx, NeuralLinkScreen.tsx, MatchTestScreen.tsx.

AuraProfile and matchmaking already wired.

VisionOS-like look in some places, but the whole app still feels inconsistent and not fully “glassy”.

Goal now:

Add a simple top navigation bar to switch between the key screens.

Make the global layout consistently VisionOS-style: deep slate gradient, glass cards, no neon.

Add a profile photo URL field in onboarding and wire it to avatarUrl (so my face / image shows in Neural Link + Match Test).

Please implement all of the following.

1) Global layout + navigation in App.tsx

Open src/App.tsx and refactor it into:

a full-page gradient background

a centered shell

a simple in-app navigation that switches between 3 screens:

Onboarding

Neural Link

Match Test

Important:

If you are already using React Router, keep it and implement a small glass nav that uses <Link>s and <Routes>.

If there is no router, use local state to switch screens, don’t introduce React Router now.

If using local state (no router)

In App.tsx:

type AuraScreen = "onboarding" | "neural" | "match";

export const App: React.FC = () => {
  const [screen, setScreen] = useState<AuraScreen>("neural");

  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-950 via-slate-900 to-black text-slate-100">
      <div className="mx-auto max-w-6xl px-4 py-6 lg:py-10">
        {/* Top nav */}
        <header className="mb-6 flex items-center justify-between gap-4">
          <div className="flex items-center gap-2">
            <div className="h-2 w-2 rounded-full bg-sky-400 animate-pulse" />
            <div className="text-[11px] font-mono tracking-[0.3em] uppercase text-slate-400">
              Aura Twin · Neural Link
            </div>
          </div>
          <nav className="flex gap-2 rounded-full bg-slate-900/70 border border-white/10 backdrop-blur-xl px-1 py-1 text-xs">
            {[
              { id: "onboarding", label: "Onboarding" },
              { id: "neural", label: "Neural Link" },
              { id: "match", label: "Match Test" },
            ].map(item => (
              <button
                key={item.id}
                onClick={() => setScreen(item.id as AuraScreen)}
                className={`px-3 py-1.5 rounded-full transition-colors ${
                  screen === item.id
                    ? "bg-slate-100 text-slate-900"
                    : "text-slate-300 hover:bg-slate-800/70"
                }`}
              >
                {item.label}
              </button>
            ))}
          </nav>
        </header>

        {/* Screen container */}
        <main className="space-y-6">
          {screen === "onboarding" && (
            <div className="rounded-3xl bg-slate-900/70 border border-white/10 backdrop-blur-2xl shadow-2xl p-4 lg:p-6">
              <OnboardingScreen />
            </div>
          )}
          {screen === "neural" && <NeuralLinkScreen />}
          {screen === "match" && (
            <div className="rounded-3xl bg-slate-900/70 border border-white/10 backdrop-blur-2xl shadow-2xl p-4 lg:p-6">
              <MatchTestScreen />
            </div>
          )}
        </main>
      </div>
    </div>
  );
};


Import useState, OnboardingScreen, NeuralLinkScreen, MatchTestScreen at the top.

Ensure the Neural Link and Match Test screens do not nest extra full-screen backgrounds, so the global gradient comes from App.tsx.

If using React Router

Keep the router logic, but wrap the <Routes> in the same header + gradient layout above.

Replace inline screen state with <Link> and NavLink components styled like the rounded pills.

Either way, result:
Top nav glass pill bar that lets me click between Onboarding, Neural Link, Match Test.

2) Make NeuralLinkScreen + MatchTestScreen respect the shell

For both NeuralLinkScreen.tsx and MatchTestScreen.tsx:

Remove any full-page gradients or min-h-screen backgrounds inside the screen component.

They should render only their internal glass cards, assuming App.tsx provides the outer gradient and padding.

Make sure they use w-full, max-w-6xl, etc only if needed; otherwise, rely on the parent container.

For Neural Link, keep the two-column “Aura Stage + Chat” layout you already created, but:

Use h-[min(80vh,720px)] or similar on the main container so it fits nicely.

Make sure scroll areas use overflow-y-auto and the custom-scrollbar class if present.

For Match Test, keep the glass card we built in the previous step, but remove any duplicate outer background/wrappers so it sits cleanly under the new header.

3) Onboarding: add Avatar URL field and wire it to AuraProfile.avatarUrl

Open src/screens/OnboardingScreen.tsx.

Find the form where you collect core fields of the user’s personality (name, introversionLevel, vibe words, etc.).

Add a new optional field:

Label: "Profile photo URL (optional)"
Helper text: "Paste a direct image link. You can add uploads later."

Implement it as a rounded glass input:

<div className="space-y-1">
  <label className="text-[11px] font-medium uppercase tracking-[0.2em] text-slate-400">
    Profile photo URL (optional)
  </label>
  <input
    type="url"
    value={avatarUrl}
    onChange={e => setAvatarUrl(e.target.value)}
    placeholder="https://…"
    className="w-full rounded-2xl bg-slate-950/60 border border-white/10 px-3 py-2 text-sm text-slate-100 placeholder:text-slate-500 focus:outline-none focus:border-slate-100/40"
  />
  <p className="text-[11px] text-slate-500">
    Aura will use this photo in the Neural Link and Match Test screens. You can leave it empty.
  </p>
</div>


Add avatarUrl to the local form state in OnboardingScreen (e.g. const [avatarUrl, setAvatarUrl] = useState("");).

When you build the AuraProfile object to pass to buildAuraProfile or save, set:

avatarUrl: avatarUrl || undefined,


Ensure that AuraProfile in types.ts already includes avatarUrl?: string (you added this earlier). If not, add it now.

Result: when I complete onboarding and create/update my profile, the avatarUrl is saved and used in:

Neural Link Stage orb (my picture instead of emoji).

Match Test cards (both your Aura card and others if they have URLs).

4) Visual consistency & polish

Please make sure of the following across the screens:

All main cards use:

rounded-3xl

bg-slate-900/70

border border-white/10

backdrop-blur-2xl

shadow-2xl

No neon colors. Use:

Background: slate-950 / slate-900

Text: slate-100 / slate-200

Accents: soft sky/blue/indigo/purple with low opacity when used as gradients.

Buttons:

Rounded-full or rounded-2xl.

Idle state calm; hover = slightly brighter; no harsh animations.

If any screen still uses old “white card with gray border” or no blur, update to the new glass pattern.

Do not change any existing Gemini API calls, types, or business logic (chat, matchmaking, etc.).
Only adjust layout, styling, navigation, and the new avatarUrl field wiring.

When finished, the app should:

Show a top glass nav with three pills: Onboarding, Neural Link, Match Test.

Use one unified deep gradient background.

Let me paste an image URL in Onboarding, then see my picture in:

Neural Link left “Aura Stage”

Match Test “Your Aura” card.