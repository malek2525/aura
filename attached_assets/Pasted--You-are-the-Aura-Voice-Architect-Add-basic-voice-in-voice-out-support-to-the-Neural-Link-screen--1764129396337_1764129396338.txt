“You are the Aura Voice Architect. Add basic voice-in / voice-out support to the Neural Link screen using browser APIs only (no new SDKs).

Create a reusable hook

Add src/hooks/useVoice.ts with a React hook that exposes:

hasSpeechSupport: boolean

hasTTSSupport: boolean

isListening: boolean

isSpeaking: boolean

lastTranscript: string

error: string | null

startListening(): void

stopListening(): void

speak(text: string): void

stopSpeaking(): void

clearTranscript(): void

Implementation details:

Use window.SpeechRecognition || window.webkitSpeechRecognition if available.

On startListening:

Clear lastTranscript.

Start recognition with interimResults = false, language en-US.

On onresult, store the final transcript and stop listening.

On onerror, set error to a friendly string and stop listening.

For TTS (speak):

Use window.speechSynthesis.

Cancel any current speech before speaking.

Choose a feminine voice if possible by scanning getVoices() for names like female, woman, amy, aria, emma, joanna, sara, luna.

Use rate = 1, pitch = 1.1, volume = 1.

VoiceControls component

Add src/components/VoiceControls.tsx:

Uses useVoice().

Props:

onFinalTranscript(text: string): void — send recognized speech into the chat.

lastAuraReply?: string — latest text reply from Aura.

UI:

If !hasSpeechSupport, show a disabled pill button saying “Voice not supported” and a small status text.

Main button:

When idle: pill button “🎙 Talk” with subtle glass style.

When listening: pill button in soft red “Listening… tap to stop”, pulsing.

Secondary small button to stop speaking (only visible when isSpeaking).

Status line under buttons:

Idle: “Voice Ready”

Listening: “● Mic live”

Speaking: “🔊 Aura speaking…”

Behavior:

useEffect watching lastTranscript: when it becomes non-empty and listening has stopped, call onFinalTranscript and then clearTranscript().

useEffect watching lastAuraReply: when it changes, call speak(lastAuraReply) (if not empty).

Wire into Neural Link chat

Find the main chat component (e.g. src/components/ChatWindow.tsx or src/screens/NeuralLinkScreen.tsx) where:

User text input is sent to Aura.

Aura responses are displayed.

Refactor the chat logic into a small hook or state so you can:

Call a sendMessage(text: string) function both from:

The text input send button

VoiceControls.onFinalTranscript

Track the last Aura text reply in state and pass it into VoiceControls as lastAuraReply.

Keep styling on-brand

Use the same glass style and rounded-full buttons as the rest of the UI.

No neon colors; use soft slate, subtle purple/blue as accents only.

Make sure the app compiles and when I click “Talk”:

The browser asks for mic permission.

I speak; when I stop, my words appear as a user chat message.

Aura replies in text and with a feminine voice that reads only her reply, not my message.”**