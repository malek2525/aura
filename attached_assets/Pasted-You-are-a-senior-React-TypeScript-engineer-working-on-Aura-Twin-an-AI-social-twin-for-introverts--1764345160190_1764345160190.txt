You are a senior React + TypeScript engineer working on Aura Twin, an AI social twin for introverts.
Tech: React + TS + Vite + Tailwind + Firebase Auth + Gemini API.
The project is already running and mostly stable (Neural Link chat, Match Score, Twin Intro, Night Debrief).
Important: Do not remove or radically rewrite working features. Refactor carefully, commit in small steps, and keep the VisionOS glass aesthetic.

GOAL

Implement and polish a new feature called “Reply Lab”:
Aura helps the user write replies to social messages (dating, friends, DMs) in three tones:

Safe / Polite

Honest / Direct

Playful / Warm

This is our key investor demo: it proves Aura is more than “just chat”.

Use the existing Gemini integration (in services/auraLLM.ts) and the current UI style.
The user is non-technical; you must do all code changes yourself.

GLOBAL RULES

Don’t break existing flows:

Login/Auth

Onboarding

Neural Link chat & voice

Match Score & Twin Intro

Night Debrief (if present)

Re-use existing patterns (glass cards, buttons, headers). Don’t introduce a new UI library.

Keep types strict. Prefer extending existing types over any.

After each major step, run the app and do a quick smoke test. Fix TypeScript errors and obvious UI bugs before moving on.

TASK 1 — Scan the codebase

Inspect:

src/App.tsx

src/types.ts

src/services/auraLLM.ts

src/screens and/or src/panels (NeuralLinkScreen, MatchTestScreen, TwinConversationScreen, NightDebriefScreen, etc.)

Confirm how navigation currently works:

In the UI there is a top nav: Talk to Aura / Match Score / Twin Intro / Reply Lab.

Make sure the "Reply Lab" button actually maps to a screen/enum value (e.g. "replyLab").

If it doesn’t exist, add a Screen/state entry for it in App.tsx in a minimal way.

Do not change behaviour yet. Just prepare notes in comments if needed.

TASK 2 — Define data types for Reply Lab

In src/types.ts add clear, typed structures:

export interface ReplyOptions {
  safe: string;
  direct: string;
  playful: string;
}

export type ReplyTone = 'safe' | 'direct' | 'playful';


If there is already something similar, reuse/extend instead of duplicating.

TASK 3 — Extend auraLLM.ts with generateReplyOptions

Open src/services/auraLLM.ts.

Re-use the existing Gemini client and environment variables. Do not create a second client with a duplicate API key.

Add a new exported async function:

import { AuraProfile, ReplyOptions } from '../types';

export async function generateReplyOptions(
  profile: AuraProfile,
  incomingText: string
): Promise<ReplyOptions> {
  // implement using the same Gemini model used for chat / matching
}


Prompt design (use this as the system / instruction content for Gemini):

You are Aura, an AI twin drafting message replies for your user.
You always respect the user’s boundaries and personality.

Input:

A structured JSON profile describing the user’s vibe, goals, and boundaries.

A single incoming message or situation the user wants to reply to.

Task:
Generate three short reply suggestions that the user can send as-is or lightly edit:

"safe": polite, low-risk, kind.

"direct": honest and clear, but still respectful.

"playful": warm, slightly flirty or humorous if appropriate.

Rules:

Never be explicit or sexual.

No hate, harassment, bullying, self-harm, or illegal advice.

If the situation is unhealthy or unsafe, instead suggest a boundary or exit.

Output strictly as JSON with keys: safe, direct, playful.

Parse the model response safely:

Try JSON.parse on the returned text.

If parsing fails, log the error and return a conservative fallback:

return {
  safe: "I'm not sure how to answer that in a safe way. Maybe take a breath and reply later.",
  direct: "I'm not comfortable replying to that right now.",
  playful: "My brain glitched on that one 😅 Let's try again with a different message."
};

TASK 4 — Create a dedicated Reply Lab panel / screen

If not already present, create a new component:

src/panels/ReplyLabPanel.tsx OR src/screens/ReplyLabScreen.tsx
(Choose whichever fits the project’s current structure better.)

UI requirements:

VisionOS glass card centered in the right column.

Title: “Reply Lab” and subtitle:

“Paste a message you want help replying to. Aura will suggest three options.”

A large textarea for the incoming message.

Optional dropdown for context (e.g. “Friend / Dating / Work”) but keep behaviour simple.

A primary button “Generate Replies”.

Loading state (spinner + text: “Aura is thinking…”).

Error state if Gemini fails.

Below, show three reply cards:

Each labeled “Safe”, “Direct”, “Playful”.

Each reply in its own glass card.

A small “Copy” or “Use this” button on each reply.

Styling:

Re-use same Tailwind classes as existing glass panels and chat bubbles (bg-slate-900/60, border-white/10, backdrop-blur-xl, rounded-3xl, etc.).

Keep typography consistent with the rest of the app.

TASK 5 — Wire Reply Lab to navigation

In App.tsx:

Ensure the Screen type / state includes "replyLab" (or similar).

Ensure the top nav button “Reply Lab” sets this screen.

In the main render switch, add a case to render the new Reply Lab component when the screen is "replyLab".

Verify:

Clicking Reply Lab doesn’t break other tabs.

Back and forth navigation keeps state where reasonable (no unnecessary resets).

TASK 6 — Connect Reply Lab to AuraProfile & generateReplyOptions

Ensure the Reply Lab component receives the current AuraProfile (from App.tsx or via context).

If there is already a hook or context for profile, use it.

Do not introduce new global state management libraries.

On “Generate Replies”:

Validate that the textarea is not empty; if empty, show a subtle hint like “Paste or type a message first.”

Call generateReplyOptions(profile, incomingText).

Store the result in local component state and render the 3 replies.

Handle loading and error states gracefully.

TASK 7 — Add a small “tone chips” UI on each reply

On each reply card:

Show a tiny label chip (e.g. SAFE, DIRECT, PLAYFUL) with subtle colors (blue, amber, pink) but no neon.

Add a hover state on each card (slight scale + border highlight).

This is just visual polish to help investors instantly see that there are different tones.

TASK 8 — Optional: integrate with chat input (light)

In NeuralLinkScreen (or main chat screen), next to the text input:

Add a subtle button/link “Open Reply Lab”.

Clicking it should:

Switch to "replyLab" screen,

Optionally pre-fill the Reply Lab textarea with the current chat input text (if any).

Keep this integration minimal; do not over-engineer.

TASK 9 — Telemetry hooks (simple)

Without adding heavy analytics libraries, add a tiny logging helper (e.g. in utils/telemetry.ts) that currently just uses console.log but can later be wired to real analytics.

Whenever:

Reply Lab is opened

generateReplyOptions is called

A user presses “Copy” / “Use this”
log structured events like:

logEvent('reply_lab_opened');
logEvent('reply_lab_generated', { length: incomingText.length });
logEvent('reply_lab_used_option', { tone: 'safe' });


This helps the founder later show investors what % of users actually use the skill.

TASK 10 — Final polish & smoke test

Run the dev server and manually test:

Login → Onboarding → Talk to Aura (chat + voice) → Match Score → Twin Intro → Reply Lab → Night Debrief (if wired).

Ensure navigation is smooth and no screen is visually broken.

Try Reply Lab with a few sample inputs:

A friendly message

A flirty dating message

A slightly rude message

Make sure the outputs:

Are different between safe/direct/playful

Respect boundaries (no explicit content)

Don’t crash when Gemini has an error.

Fix any TypeScript errors, missing imports, or obvious layout glitches you encounter.

Once all 10 tasks are complete and the app is stable, stop.