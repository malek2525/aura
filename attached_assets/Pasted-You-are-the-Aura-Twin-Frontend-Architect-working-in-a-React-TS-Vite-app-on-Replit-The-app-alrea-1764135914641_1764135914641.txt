You are the Aura Twin Frontend Architect working in a React + TS + Vite app on Replit.
The app already has:

src/types.ts with AuraProfile & related types.

src/services/auraLLM.ts with functions like buildAuraProfile, chatWithAura, and matchAuras (or similar).

src/screens/MatchTestScreen.tsx that shows a basic “Neural Twin Match Simulation” between “Your Aura” and a potential match.

Onboarding already builds a profile and Neural Link chat works.

We want to upgrade matchmaking + UI to feel more like a real twin-to-twin chemistry test and less like a basic sample.
Keep the entire visual style VisionOS / iOS 18 inspired:

Deep slate background

Glass cards with blur

Very soft gradients (no neon)

Rounded-3xl corners everywhere

Please implement all the changes below.

1) Enrich AuraProfile (types.ts) – add photo + social DNA

Open src/types.ts and extend the main profile type used for Aura (likely AuraProfile).

Add fields (keeping existing ones):

export interface AuraProfile {
  id: string;
  name: string;
  age?: number;
  pronouns?: string;
  city?: string;
  country?: string;
  avatarUrl?: string;          // URL to profile image (optional)

  introversionLevel: "low" | "medium" | "high";
  vibeWords: string[];         // e.g. ["calm", "playful", "nerdy"]
  socialSpeed: "slow" | "medium" | "fast";
  relationshipIntent: "friends" | "dating" | "unsure";
  hardBoundaries: string[];
  softPreferences?: string[];

  summary: string;             // 1–2 sentence description generated by LLM
}


If there is already an AuraProfile type, merge these fields instead of replacing.
Ensure anything that breaks is updated accordingly (Onboarding, Neural Link, etc.).

Add a small helper type for match results:

export interface AuraMatchResult {
  compatibilityScore: number; // 0–100
  matchLabel: "Low" | "Medium" | "High";
  summary: string;
  whyItWorks: string[];
  watchOut: string[];
  vibeDescription: string;
  suggestedFirstMessage: string;
}


Export it from types.ts.

2) Improve matchAuras in services/auraLLM.ts

Open src/services/auraLLM.ts.
We need a single function that calls Gemini and returns the structured AuraMatchResult.

If there is already a matchAuras function, replace / refactor it to:

import { AuraProfile, AuraMatchResult } from "../types";

export async function matchAuras(
  yourAura: AuraProfile,
  otherAura: AuraProfile
): Promise<AuraMatchResult> {
  // ...
}


Implementation requirements:

Use the existing Gemini client setup in this file. Prefer a fast model (e.g. gemini-2.5-flash / flash-lite) to avoid quota issues.

Build a system prompt like:

You are an expert in social chemistry and emotional compatibility for introverts.
You read two Aura Twin profiles and evaluate:
- emotional fit
- conversational rhythm
- risk areas

You must output STRICT JSON in the exact schema provided.
Do not include any extra text or comments.


In the user content, send a compact JSON of both profiles:

{
  "auraA": { ...yourAura },
  "auraB": { ...otherAura }
}


Ask Gemini to respond with only:

interface AuraMatchResult {
  compatibilityScore: number; // 0–100
  matchLabel: "Low" | "Medium" | "High";
  summary: string;
  whyItWorks: string[];
  watchOut: string[];
  vibeDescription: string;
  suggestedFirstMessage: string;
}


Set responseMimeType: "application/json" in the model call.

Parse the response safely:

Try JSON.parse(text) in a try/catch.

If parsing fails, log it and return a sensible fallback:

return {
  compatibilityScore: 50,
  matchLabel: "Medium",
  summary: "Aura feels a cautious medium match. There is potential if both are patient.",
  whyItWorks: ["Both value depth over small talk."],
  watchOut: ["May overthink each other's silence."],
  vibeDescription: "Softly curious with some emotional caution.",
  suggestedFirstMessage: "Hey, want to trade one thing we’re secretly nerdy about?"
};

3) Add some demo profiles for testing

Create a new file: src/services/demoProfiles.ts with a few hard-coded AuraProfile objects.
These are just for the “Match Test” screen so I can simulate matches without a full backend.

Example:

import { AuraProfile } from "../types";

export const demoProfiles: AuraProfile[] = [
  {
    id: "you",
    name: "malek",
    age: 26,
    city: "Budapest",
    country: "Hungary",
    avatarUrl: "https://images.pexels.com/photos/415829/pexels-photo-415829.jpeg",
    introversionLevel: "medium",
    vibeWords: ["calm", "curious", "romantic"],
    socialSpeed: "medium",
    relationshipIntent: "dating",
    hardBoundaries: ["disrespect", "ghosting"],
    softPreferences: ["deep talks at night", "creative people"],
    summary: "Calm but passionate, loves building things and connecting with people who feel safe and honest."
  },
  {
    id: "lina",
    name: "Lina",
    age: 24,
    city: "Istanbul",
    country: "Turkey",
    avatarUrl: "https://images.pexels.com/photos/415829/pexels-photo-415829.jpeg",
    introversionLevel: "high",
    vibeWords: ["soft", "playful", "introverted"],
    socialSpeed: "slow",
    relationshipIntent: "dating",
    hardBoundaries: ["loud parties", "pressure to open up fast"],
    softPreferences: ["texting late", "anime", "quiet cafés"],
    summary: "Soft, shy, and playful once she trusts you. Needs emotional safety more than anything."
  },
  // add 2–3 more sample profiles…
];


These URLs can be generic stock photos; they are just placeholders.

4) Refactor MatchTestScreen.tsx to VisionOS glass and use matchAuras

Open src/screens/MatchTestScreen.tsx and completely refactor the UI to:

Use:

demoProfiles as selectable profiles

matchAuras() to calculate a real match result when I click a button.

Layout:

Full-width glass card with:

Top: Title + small description

Middle: 2 avatar cards side-by-side (your Aura vs selected match)

Center: big compatibility % with label “Low / Medium / High”

Bottom: two columns “Why it works” and “Things to watch”, plus “Suggested first message”.

Implementation details:

Import hooks + types:

import React, { useState } from "react";
import { AuraProfile, AuraMatchResult } from "../types";
import { demoProfiles } from "../services/demoProfiles";
import { matchAuras } from "../services/auraLLM";


Assume the first profile in demoProfiles is “you” by default, and let the user pick the other side from a select dropdown or pill buttons on the right.

Outer container:

<div className="w-full h-full flex items-start justify-center">
  <div className="w-full max-w-5xl rounded-3xl bg-slate-900/70 border border-white/10 backdrop-blur-2xl shadow-2xl p-6 lg:p-8 space-y-6">
    {/* content */}
  </div>
</div>


Aura avatar cards for each side:

function AuraCard({ profile, label }: { profile: AuraProfile; label: string }) {
  return (
    <div className="flex flex-col items-center gap-3 rounded-3xl bg-slate-900/70 border border-white/10 px-4 py-5 shadow-lg">
      <div className="relative h-24 w-24 rounded-full overflow-hidden bg-slate-800 flex items-center justify-center">
        {profile.avatarUrl ? (
          <img src={profile.avatarUrl} alt={profile.name} className="h-full w-full object-cover" />
        ) : (
          <div className="text-3xl">🟣</div>
        )}
      </div>
      <div className="text-xs uppercase tracking-[0.2em] text-slate-500">{label}</div>
      <div className="text-lg font-semibold text-slate-100">{profile.name}'s Aura</div>
      <p className="text-[11px] text-slate-300 text-center leading-relaxed max-w-xs">
        {profile.summary}
      </p>
    </div>
  );
}


In MatchTestScreen, manage state:

const [yourAura] = useState<AuraProfile>(demoProfiles[0]);
const [selectedId, setSelectedId] = useState<string>(demoProfiles[1]?.id ?? "");
const [matchResult, setMatchResult] = useState<AuraMatchResult | null>(null);
const [isLoading, setIsLoading] = useState(false);
const potentialMatch = demoProfiles.find(p => p.id === selectedId)!;


Run match on button click:

const handleRunMatch = async () => {
  if (!potentialMatch) return;
  setIsLoading(true);
  try {
    const res = await matchAuras(yourAura, potentialMatch);
    setMatchResult(res);
  } finally {
    setIsLoading(false);
  }
};


Match summary UI (center section):

<div className="flex flex-col items-center gap-2">
  <div className="text-[11px] tracking-[0.25em] uppercase text-slate-500">
    Compatibility
  </div>
  <div className="text-5xl font-bold text-slate-100">
    {matchResult ? `${matchResult.compatibilityScore}%` : "--"}
  </div>
  <div className="text-sm font-mono text-slate-300">
    {matchResult ? matchResult.matchLabel + " match" : "Run a match to see."}
  </div>
  {matchResult && (
    <p className="mt-2 text-sm text-slate-200 text-center max-w-xl">
      {matchResult.summary}
    </p>
  )}
  <button
    onClick={handleRunMatch}
    disabled={isLoading}
    className="mt-4 rounded-full px-6 py-2 bg-slate-100 text-slate-900 text-sm font-semibold hover:bg-white disabled:opacity-60 transition-colors"
  >
    {isLoading ? "Matching twins..." : "Run Twin Match"}
  </button>
</div>


Below, show 2 glass columns for “Why it works” and “Things to watch”, plus the suggested opener:

{matchResult && (
  <div className="grid grid-cols-1 lg:grid-cols-2 gap-4 mt-6">
    <div className="rounded-2xl bg-slate-900/70 border border-white/10 p-4 space-y-2">
      <div className="text-xs uppercase tracking-[0.2em] text-slate-500 mb-1">
        Why it works
      </div>
      <ul className="list-disc list-inside text-sm text-slate-200 space-y-1">
        {matchResult.whyItWorks.map((item, idx) => (
          <li key={idx}>{item}</li>
        ))}
      </ul>
    </div>
    <div className="rounded-2xl bg-slate-900/70 border border-white/10 p-4 space-y-2">
      <div className="text-xs uppercase tracking-[0.2em] text-slate-500 mb-1">
        Things to watch
      </div>
      <ul className="list-disc list-inside text-sm text-slate-200 space-y-1">
        {matchResult.watchOut.map((item, idx) => (
          <li key={idx}>{item}</li>
        ))}
      </ul>
    </div>
  </div>
)}

{matchResult && (
  <div className="mt-4 rounded-2xl bg-slate-900/80 border border-white/10 p-4">
    <div className="text-xs uppercase tracking-[0.2em] text-slate-500 mb-2">
      Suggested first message
    </div>
    <p className="text-sm text-slate-100 bg-slate-900/80 rounded-2xl px-4 py-3 border border-white/5">
      {matchResult.suggestedFirstMessage}
    </p>
  </div>
)}


Above the cards, add a small selector to choose which demo profile to match against (except “you”):

<div className="flex items-center gap-3 mb-4">
  <span className="text-xs uppercase tracking-[0.2em] text-slate-500">
    Potential match
  </span>
  <select
    className="rounded-full bg-slate-900/80 border border-white/10 px-3 py-1.5 text-xs text-slate-100"
    value={selectedId}
    onChange={(e) => setSelectedId(e.target.value)}
  >
    {demoProfiles
      .filter(p => p.id !== yourAura.id)
      .map((p) => (
        <option key={p.id} value={p.id}>
          {p.name} – {p.city}
        </option>
      ))}
  </select>
</div>


Make sure the overall screen still fits inside the main app layout and uses the same background as Neural Link.

5) Keep everything consistent

Do not break the existing Neural Link chat or Onboarding flows.

If any existing code expects old fields on AuraProfile, update it to handle the new ones safely (using optional chaining etc.).

Keep all colors in the slate / soft purple / soft blue family, no bright neon.

When finished, ensure the app builds in Vite and:

On the Match Test tab I can select another profile, click “Run Twin Match”, see:

My Aura card

Other person’s Aura card

Big % + label

Why it works / Things to watch

Suggested first message

Avatars show the avatarUrl images in circular glass frames.