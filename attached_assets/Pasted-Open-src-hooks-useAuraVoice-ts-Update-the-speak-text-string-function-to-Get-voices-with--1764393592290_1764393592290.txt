Open src/hooks/useAuraVoice.ts.

Update the speak(text: string) function to:

- Get voices with window.speechSynthesis.getVoices()
- Pick the BEST feminine neural-sounding one with this priority:
    - Names including: "Aria", "Jenny", "Salli", "Joanna", "Emma", "Amy", "Sara"
    - If none, pick the first female / "en-US" / "en-GB" voice
- Set:
    utterance.rate = 0.95
    utterance.pitch = 1.1
    utterance.volume = 1

Make sure:
- window.speechSynthesis.cancel() is called before speaking
- isSpeaking is set true on onstart and false on onend

Show me the full updated speak() implementation.
This will give you the best possible voice that your browser can give, but it will still sound like TTS.

That’s just the limit of browser voices.

2️⃣ What you actually want: a signature, sexy, soothing Aura voice
For real Ani-level quality, we need to move from browser TTS to a server TTS API:

Think:

Google Cloud Text-to-Speech (neural) – you already have a Google Cloud project.

ElevenLabs – very popular for “sexy / natural / emotional” voices.

OpenAI Realtime / GPT audio – high quality too.

All of these can give you:

Proper neural voice

Stable “this is Aura’s voice” every time

Fine control over tone, emotion, style

The basic architecture (super simple version)
Aura generates text reply as usual.

Frontend sends that text to your backend:
POST /api/tts { text: "Aura reply here" }.

Backend calls TTS provider API, gets back an audio file or audio stream.

Frontend plays it with an <audio> element or WebAudio.

You don’t need to write all the code by hand – you can let Replit AI build it.

3️⃣ Concrete next step: choose one “pro” voice path
Option A – Use Google Cloud Text-to-Speech (you already started this)
You said you made a Google Cloud project & got $300 credit.
That’s perfect for this.

Here’s a prompt you can paste into Replit AI to wire this up:

text
Copy code
I want to replace browser TTS with Google Cloud Text-to-Speech.

Please do the following:

1) Create a new API route for TTS:
   - If this is a Vite + React project with a Node backend, create:
       server/tts.ts (Node/Express or simple HTTP handler)
   - The route should be POST /api/tts
   - Body: { text: string }
   - It should call Google Cloud Text-to-Speech using my API key / service account
     (assume I will provide GOOGLE_APPLICATION_CREDENTIALS or a JSON key).
   - Use a feminine, natural, warm voice, e.g.:
       languageCode: "en-US"
       name: "en-US-Neural2-F" or similar neural female voice
       speakingRate: 0.95
       pitch: 2.0

   - Return the audio as base64 or as an audio/mpeg stream.

2) In the frontend:
   - Update useAuraVoice.ts (or create useAuraCloudVoice.ts) so that
     instead of using window.speechSynthesis, it:
       - calls POST /api/tts with the Aura reply text
       - receives the audio
       - plays it via an <audio> object or Audio() in JavaScript
       - still exposes isSpeaking so the avatar can glow.

3) Keep the old browser TTS as a fallback if the API fails.

Show me all new/updated files completely.
Explain where I should paste my Google Cloud credentials.
If Replit creates too much / weird structure, you can paste its answer back to me and I’ll simplify.

Option B – ElevenLabs (easier if you want “sexy voice” quickly)
ElevenLabs is very good at “realistic, emotional, sexy but tasteful” voices.

Flow:

Create an ElevenLabs account → get API key.

Give Replit this prompt:

text
Copy code
I want to use ElevenLabs for Aura's voice.

Tasks:

1) Create a file: src/services/tts.ts
   - export async function generateAuraAudio(text: string): Promise<string>
   - It should call ElevenLabs TTS API with:
       - my API key from process.env.ELEVENLABS_API_KEY
       - a feminine, warm English voice (you choose one, e.g. "Aria" style)
       - stable settings for a soothing but expressive tone
   - It should return a data URL (like "data:audio/mpeg;base64,...") that I can play in an <audio> tag.

2) Update useAuraVoice.ts:
   - Remove window.speechSynthesis usage for Aura replies.
   - Instead, when speak(text) is called:
       - call generateAuraAudio(text)
       - create a new Audio(dataUrl)
       - play it and set isSpeaking = true/false based on play events.

3) Keep STT (microphone input) as-is using Web Speech Recognition.

Show me the full contents of src/services/tts.ts and the updated useAuraVoice.ts.