You are my frontend copilot for the Aura Twin MVP.
Tech: React + TypeScript + Tailwind + Vite.
Files already exist:

src/components/AuraAvatar.tsx (current simple head)

src/components/AuraStage.tsx

src/index.css with aura animations

I want you to upgrade Aura’s look to Aura v2 – a holographic twin, closer in feeling to Annie from xAI:

elegant

soft

simple but premium

feels like a hologram inside glass, not a cartoon emoji.

HARD CONSTRAINTS

Do NOT break any imports or props for AuraAvatar. All existing usages across the app must keep working.

Keep using Tailwind + our VisionOS / glass style (dark slate, soft gradients, no neon).

No anime / childish style, no big goofy facial features, no over-sexualization. Think “calm 20-something digital assistant”, gender-soft, non-thirsty, but attractive and soothing.

1) Upgrade AuraAvatar.tsx

Open src/components/AuraAvatar.tsx. Keep the same prop types (especially anything using AuraState / mood / moodIntensity / name / vibeWords).

Replace the current very simple face with a layered holographic character:

Outer layer: circular glass shell (thin border, subtle specular highlight).

Inner layer: gradient background that changes with mood.

Character: bust / shoulders silhouette with:

Soft jawline

Subtle nose + lips

Simple hair shape (not detailed strands, just a clean silhouette)

Eyes that are small and calm, not huge cartoon eyes

Everything should be built with Tailwind + CSS gradients / borders / pseudo-elements – no external images yet.

Use multiple layers to give depth: one for hair, one for face, one for shoulders, maybe a soft glow layer behind the head.

Add these animation hooks:

Main container uses .animate-aura-living (float + breathe).

Eyes or eyelids use .animate-aura-blink so Aura blinks gently every few seconds.

Optionally, add a very slow “light sweep” across the glass shell using a pseudo-element and keyframes.

Map mood → style:

calm → cool blue / violet gradients, relaxed subtle glow

happy → warmer rose / peach gradient, slightly brighter cheeks

anxious → more muted amber / violet, slightly dimmer overall

sad → deeper blue / indigo, softer lighting

neutral / default → balanced slate / blue gradient

Use moodIntensity to slightly increase or decrease glow/brightness, but keep everything soft. No neon.

2) Improve animations in index.css

Open src/index.css and make sure we have these keyframes (add or refine as needed):

@keyframes aura-breathe – scale 0.97 → 1.03 over 4–5s, ease-in-out, infinite.

@keyframes aura-float – translateY(0) → -10px → 0 over 6s, ease-in-out, infinite.

@keyframes aura-blink – for eyelids: mostly open, briefly closed every 7–10s.

Utility classes:

.animate-aura-breathe { animation: aura-breathe 5s ease-in-out infinite; }
.animate-aura-float   { animation: aura-float 6s ease-in-out infinite; }
.animate-aura-living  { animation: aura-breathe 5s ease-in-out infinite,
                                      aura-float 7s ease-in-out infinite; }
.animate-aura-blink   { animation: aura-blink 8s ease-in-out infinite; }


Use these classes in AuraAvatar.tsx instead of inline keyframes.

3) Wire with AuraStage.tsx

Open src/components/AuraStage.tsx and confirm it still imports and uses AuraAvatar.

Make sure AuraStage passes the current AuraState (mood + moodIntensity) into AuraAvatar so all the new mood effects actually work.

Do not change the rest of the layout (Neural Link / Match Score / Twin Intro / Reply Lab panes) – only improve the avatar’s visuals.

4) Final checks

The app must compile with no TS errors.

All existing flows (onboarding, Talk to Aura, Match Score, Twin Intro, Reply Lab) must work exactly as before.

The new avatar should look like a holographic assistant living inside the sphere, blinking, floating, and gently glowing with mood.

If something breaks, fix it instead of removing features.

When you finish, run the dev server and confirm the avatar renders correctly in all three main tabs.