You are my Replit agent working on the “Aura Twin” React + Vite + TypeScript app.

After adding Firebase authentication, a few regressions happened:

- The Sign Out button is only visible during onboarding, not when I am inside the main Aura experience.
- The top segmented navigation tabs (e.g. “Talk to Aura”, “Match Score”, “Twin Intro”, “Reply Lab”) disappeared.
- The layout feels too tall and not well-balanced on the screen.

I do NOT want to edit any code myself. Please inspect and update the codebase for me.

=== GOAL BEHAVIOR ===
When a user is logged in:

1) At the very top of the app there should be a **header bar** with:
   - Left side: small Aura Twin logo/text (“Aura Twin”).
   - Center: a pill-style segmented control with these tabs:
       - "Talk to Aura"
       - "Match Score"
       - "Twin Intro"
       - "Reply Lab"
   - Right side: 
       - a subtle “Night Mode 🌙” button (if we already had it before) 
       - a **Sign Out** button that calls signOut() from AuthContext.

2) That header must be visible:
   - On Neural Link / talking screen
   - On Match screen
   - On Twin Intro screen
   - On Reply Lab screen
   - Basically, anytime the user is past onboarding and not on a dedicated full-screen night mode.

3) Clicking each tab should:
   - “Talk to Aura” -> show the existing NeuralLinkScreen content (chat + avatar).
   - “Match Score” -> show the existing MatchTestScreen.
   - “Twin Intro” -> show the existing TwinConversationScreen or Twin intro view.
   - “Reply Lab” -> show the existing “Reply” tool screen if we have one, or at minimum a placeholder panel that says “Reply Lab coming soon”.

4) The Sign Out button should **always** be visible in the header whenever user is logged in, not just during onboarding. When I click it, it should:
   - Call signOut() from the AuthContext
   - Return me to AuthScreen (login screen).

5) The layout should be visually balanced:
   - Main container max-width ~ 1200–1280px and centered.
   - Reasonable padding on top and bottom so it doesn’t feel cramped but also not stretched.
   - On desktop, avatar + content should sit nicely above the fold without needing to scroll to see chat input.

=== IMPLEMENTATION STEPS ===

1) Open src/context/AuthContext.tsx
   - Confirm that useAuth() exposes: { user, loading, signOut, ... }.
   - Do NOT move signOut logic; just ensure it’s available.

2) Open src/App.tsx
   - After handling loading and user null:
       - If !user -> return <AuthScreen /> (this is already working, keep it).
       - If user exists -> render a layout with:
           - A header bar at the top (as described above)
           - Below it, the main content which switches based on an “activeTab” state.
   - Introduce a local state in AppContent (or similar):
       const [activeTab, setActiveTab] = useState<'talk' | 'match' | 'intro' | 'reply'>('talk');
   - Wire the segmented control buttons to setActiveTab('talk' | 'match' | 'intro' | 'reply').

   - For the main content:
       - if activeTab === 'talk' -> render the existing NeuralLinkScreen with the current props (profile, chatHistory, auraState, etc.).
       - if activeTab === 'match' -> render MatchTestScreen.
       - if activeTab === 'intro' -> render TwinConversationScreen (or the Twin intro UI we used before).
       - if activeTab === 'reply' -> render the existing Reply Lab screen if one exists (search for “Reply Lab” or reply-related component) or a simple placeholder.

   - Make sure the Sign Out button is inside this header (top-right), and it calls signOut() from useAuth().

3) Restore the previous tab labels
   - Search the entire project for the strings "Talk to Aura", "Match Score", "Twin Intro", "Reply Lab".
   - If an older segmented control or header component already exists (for example in a screen component), you can reuse that JSX structure and move it into the main App header so it’s shared across all tabs.

4) Layout + styling adjustments
   - Wrap the whole authenticated content in a container like:
       <div className="min-h-screen bg-gradient-to-br from-slate-950 via-slate-900 to-black text-slate-100">
         <div className="max-w-6xl mx-auto px-4 py-6 flex flex-col gap-6">
           {/* header + main */}
         </div>
       </div>
   - Ensure the chat area in “Talk to Aura” does not extend too tall: let the main panel have rounded corners and a max height with internal scroll if needed, so it feels like a card rather than an endless page.

5) Do NOT delete the onboarding flow
   - Keep the behavior:
       - If profile is missing -> show onboarding wizard.
       - After onboarding completes -> go into the main tabbed experience with header + Sign Out.

6) After changes, verify:
   - When I load the app while logged IN, I see:
       - Header with tabs + Sign Out in the top-right.
       - Default tab is “Talk to Aura” (chat).
   - I can click “Match Score”, “Twin Intro”, “Reply Lab” and see content change.
   - I can always click “Sign Out” in the top-right and it returns me to the AuthScreen.

Finally, summarize:
- Which files you changed
- How I can switch between the four tabs
- Exactly where the Sign Out button now lives in the UI.