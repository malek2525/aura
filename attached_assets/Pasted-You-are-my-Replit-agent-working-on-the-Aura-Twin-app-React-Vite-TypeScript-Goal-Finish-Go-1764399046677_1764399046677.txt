You are my Replit agent working on the Aura Twin app (React + Vite + TypeScript).

Goal: Finish Google Cloud Text-to-Speech integration using the API key stored in Secrets as GOOGLE_TTS_API_KEY, and use a single high-quality female neural voice that sounds natural and soothing (en-US-Neural2-H).

Current context:
- This is a Vite + React front-end.
- We already have voice UI (Speak button + useAuraVoice / LiveVoiceMode) but it was using Gemini Live and is unstable.
- I want a simpler, reliable TTS pipeline through Google Cloud Text-to-Speech.

### TASKS

1. Search the repo for any existing TTS or `useAuraVoice` implementation. Reuse / refactor it instead of creating duplicates.

2. Create OR update a lightweight backend endpoint in this Repl at `/api/tts` that:
   - Runs on the existing Node/Express (or Vite dev server middleware) backend for this Repl.
   - Reads the API key from `process.env.GOOGLE_TTS_API_KEY` (never hard-code the key).
   - Accepts `POST` with JSON body: `{ "text": "string" }`.
   - Calls `https://texttospeech.googleapis.com/v1/text:synthesize?key=${API_KEY}`.
   - Request JSON body should be:

     ```json
     {
       "input": { "text": "TEXT_GOES_HERE" },
       "voice": {
         "languageCode": "en-US",
         "name": "en-US-Neural2-H"
       },
       "audioConfig": {
         "audioEncoding": "MP3",
         "speakingRate": 0.9,
         "pitch": 2.0
       }
     }
     ```

   - Returns `{ "audioContent": "<base64 string>" }` to the frontend.
   - Handle and log errors gracefully.

3. In the frontend, create or update `src/hooks/useAuraVoice.ts` so that it:
   - Exports: `startListening`, `stopListening`, `speak`, `isListening`, `isSpeaking`, and any other useful state.
   - Uses browser SpeechRecognition (if available) for STT, similar to the current implementation.
   - For TTS, sends `POST /api/tts` with `{ text }`, reads `audioContent` (base64), decodes it to a `Blob`, and plays it via `Audio` or `AudioContext`.
   - Sets `isSpeaking = true` while audio is playing, and `false` when finished or on error.

4. Wire the hook into `NeuralLinkScreen`:
   - When Aura sends a reply message, call `speak(replyText)` so Aura always speaks her reply.
   - The “Speak” / microphone button should:
     - Start or stop listening (STT) only.
     - Not stop TTS playback.
   - Pass `isSpeaking` into `AuraAvatar` (or the avatar component) as `isSpeaking={isSpeaking}` so the glow animation activates while Aura talks.

5. Make sure:
   - The API key is only used on the server side, never exposed in the client bundle.
   - Errors from `/api/tts` or audio playback are caught. Show a subtle status like:
     > “Voice is temporarily offline, showing text only.”
     instead of breaking the UI.

6. Run the app and verify:
   - Typing and sending a message to Aura still works.
   - The browser plays natural-sounding audio for Aura’s reply using the Google TTS voice.
   - The avatar glows or pulses while the audio is playing.

Finally, describe briefly:
- Which files you created or changed.
- How to change the TTS voice later (e.g. by editing the `voice.name` in the backend).